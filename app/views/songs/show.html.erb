<h1><%= link_to @song.title, song_path(@song) %> - <%= display_artist @song %></h1>


<% if !@song.artist %>
    <%= link_to :"Add Artist", edit_song_path(@song) %>
<% else %>
    <h2><%= link_to @song.artist_name, @song.artist %></h2>
<% end %>


<%= link_to "All Songs", songs_path %>
